pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Clarusway reinvent yourself'
                echo "Hi, Everybody"
                sh 'echo Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile'
            }
        }
    }
}
